# 3. README DETALLADO (FORMATO COMPACTO)
    readme_content = f"""
# 🏭 {project_name} | RPK v7.0 Industrial Standard

### 🚀 EJECUCIÓN RÁPIDA
- **Python Runtime (RED):** `{PYTHON_PORTABLE}`
- **Database:** SQLite Local (prohibido servicios Cloud).
- **UI Standard:** RPK Red (#E30613) | Dark Mode.

---

### 🛠️ PROTOCOLO DE TRABAJO (SOP)
Cualquier modificación debe seguir este flujo estrictamente para evitar fallos de integridad:

1. **VALIDAR:** Ejecutar auditoría de sintaxis y patrones.
   > `{PYTHON_PORTABLE} scripts/qa_scanner.py`
2. **CERRAR TAREA:** Sincronizar con GitHub y auditar automáticamente.
   > `{PYTHON_PORTABLE} scripts/ops_sync.py "tu mensaje"`

---

### 📂 ARQUITECTURA DEL SISTEMA
- `backend/`: Lógica central y base de datos local.
- `frontend/`: Interfaz de usuario industrial.
- `scripts/`: Herramientas de automatización (QA, Sync, Check).
- `.agent/`: Reglas de comportamiento para la IA.

---

### 🤖 REGLAS PARA LA IA (AGENT RULES)
- **NO** uses el comando `python` global; usa siempre la ruta portable de red.
- **NO** subas cambios a GitHub manualmente; usa siempre `ops_sync.py`.
- **SIEMPRE** consulta el Contexto 7 antes de proponer cambios estructurales.
- **VALIDA** cada script con el scanner AST antes de reportar "Terminado".

---
*Generated by Génesis RPK v7.0 - Industrial Agentic System*
    """